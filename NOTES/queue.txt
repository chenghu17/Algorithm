
// c++中queue使用积累

    队列（queue）是一种线性存储结构，队列中数据按照"先进先出"的方式进出队列，
        向队列中添加数据时，是在其尾部进行操作，删除数据时，只能对队列前面进行操作。
    队列通常包含以下操作：
        push  -- 向队列尾部中添加元素
        front   -- 返回队列开头元素
        pop   -- 返回并删除队列前面元素
        empty()   -- 判断队列是否为空
        size()  --  返回队列的大小