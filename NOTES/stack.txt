
// c++中stack使用积累

    栈（stack）是一种线性存储结构，栈中数据按照"后进先出"的方式进出栈，
        向栈中添加/删除数据时，只能对栈顶元素进行操作。
    栈通常包含三种操作：
        push  -- 向栈中添加元素
        top   -- 返回栈顶元素
        pop   -- 返回并删除栈顶元素

    栈的底层实现：C++使用数组实现的栈，能存储任意类型的数据。借助Template<class T>。
